services:
  db:
    image: postgres:15
    container_name: postgres_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: myapi_db
    ports:
      - "5432:5432"
    volumes:
      - ases_db_data:/var/lib/postgresql/data

volumes:
  ases_db_data: